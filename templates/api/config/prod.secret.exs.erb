use Mix.Config

config :joken,
  secret_key: System.get_env("SESSION_SECRET"),
  json_module: <%= module_name %>.JSON

config :<%= project_slug %>, <%= module_name %>.Repo,
  url: System.get_env("DATABASE_URL"),
  adapter: Ecto.Adapters.Postgres,
  extensions: [{Extensions.JSON, []}]
