use Mix.Config

config :<%= project_slug %>, <%= module_name %>.Endpoint,
  http: [port: 4001],
  server: false

config :logger, level: :warn

config :<%= project_slug %>, <%= module_name %>.Repo,
  adapter: Ecto.Adapters.Postgres,
  extensions: [{Extensions.JSON, library: Poison}],
  database: "<%= project_slug %>_test",
  size: 1,
  max_overflow: false
