require 'spec_helper'

describe Phoenix::Generate do
  it 'has a version number' do
    expect(Phoenix::Generate::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
